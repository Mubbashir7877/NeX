<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pck.nex">

    <!-- Android 13+ (Tiramisu): post notifications runtime permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 12+ (S): schedule exact alarms for task due-times -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".NeXApp"
        android:allowBackup="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:icon="@mipmap/ic_launcher"
        android:label="NeX"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeX">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver for exact task alarms -->
        <receiver
            android:name=".notifications.TaskAlarmReceiver"
            android:exported="false" />

        <!-- No manifest setup needed for WorkManager; it wires itself. -->
        <receiver
            android:name=".widget.NeXTodayWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.pck.nex.widget.ACTION_REFRESH" />
                <action android:name="com.pck.nex.widget.ACTION_TOGGLE_DONE" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nex_today_widget_info" />
        </receiver>

        <service
            android:name=".widget.NeXWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />


        



    </application>
</manifest>
